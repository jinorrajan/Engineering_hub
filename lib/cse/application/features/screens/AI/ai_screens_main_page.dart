import 'package:flutter/material.dart';

class AiScreenmainPage extends StatelessWidget {
  const AiScreenmainPage({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}